<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="CameraTheme" parent="CameraTheme.Base">

    </style>

    <style name="CameraTheme.Base" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="CameraTheme.AppBarOverlay"
        parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="CameraTheme.PopupOverlay"
        parent="ThemeOverlay.AppCompat.Light" />

    <style name="CameraTheme.NoUI" parent="CameraTheme.Base">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowIsFloating">true</item>
    </style>
</resources>